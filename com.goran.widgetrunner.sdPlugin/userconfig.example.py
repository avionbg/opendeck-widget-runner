"""Copy this file to ``userconfig.py`` and edit it with your own paths.

``userconfig.py`` is git-ignored; the values here override the placeholders in ``config.py``.
Only set what you need — anything you leave out keeps the generic default.
"""

# Path to mpv (portable builds are not on PATH):
# MPV_PATH = r"C:\Programs\mpv\mpv.exe"

# A local .m3u playlist and an optional group-title filter:
# M3U_PATH = r"C:\Users\me\radio\stations.m3u"
# M3U_GROUP = "My stations"

# Your Android TV's IP address (adb network debugging must be enabled on the TV):
# TV_IP = "192.168.1.50"

# Your contact for the MusicBrainz User-Agent (a project URL or an e-mail):
# MUSICBRAINZ_CONTACT = "https://github.com/yourname/opendeck-widget-runner"
